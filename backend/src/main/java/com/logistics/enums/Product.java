package com.logistics.enums;

public class Product {
    public enum ProductType {
        FRESH,
        LETTER,
        GOODS
    }

    public enum ProductStatus {
        ACTIVE, 
        INACTIVE
    }
}
